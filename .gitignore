# Node.js
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
# package-lock.json # Usually committed, remove if not intended

# Build files
dist/
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
logs/
*.log

# OS specific files
.DS_Store
Thumbs.db

# Editor directories and files
.idea/
# .vscode/ # More specific rules are better, see below
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# env/ # Covered by general venv/
# venv/ # Covered by general venv/
# ENV/ # Covered by general venv/

# Confluence credentials file (if stored locally and not in .env)
confluence.json 

# General Dependencies (can be more specific if needed)
# /node_modules # Already covered at the top
# /.pnp
# .pnp.js

# Testing
coverage/

# Production
# /build # Already covered at the top

# Misc
# .DS_Store # Already covered
*.pem

# Logs (duplicates, covered above)
# npm-debug.log*
# yarn-debug.log*
# yarn-error.log*

# Environment Variables (duplicates)
# .env*.local

# Editor directories and files (refined rules)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace

# --- BRD Generator Specific --- 

# Output folders for generated documents and intermediary files from server
# These paths should be relative to the .gitignore file (project root)
server/generated/
server/uploads/

# Output folders for client-side generated/downloaded content (if any, adjust path if needed)
# client/generated/
# client/uploads/

# General generated and uploads folders at project root (as requested)
generated/
uploads/

# Configuration files that might contain sensitive info if not handled by env vars
# server/config.json # Example

# Specific user data or temp files
*.brd.json
*.temp
*.backup

# AI Model related files (if any are downloaded or cached locally)
# *.pth
# *.bin

# Python virtual environment (consolidated)
.venv/
venv/
ENV/
env/

# Python cache (already covered)
# __pycache__/
# *.py[cod]
# *$py.class

# Jupyter Notebook checkpoints
.ipynb_checkpoints